{
  "name": "npm-dependency-duplications",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "dependencies": {
    "@actions/core": "1.6.0",
    "@actions/exec": "1.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1",
    "eslint": "^7.32.0",
    "eslint-config-vazco": "^6.2.0",
    "jest": "^27.2.5"
  }
}
